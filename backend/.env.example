# Database Configuration
DB_HOST=localhost
DB_PORT=3306
DB_NAME=device_sandbox
DB_USER=root
DB_PASS=

# Application Configuration
APP_ENV=development
APP_DEBUG=true

# CORS Configuration (update with your React dev server URL)
CORS_ORIGIN=http://localhost:3000

# API Configuration
API_PREFIX=/api
API_VERSION=v1